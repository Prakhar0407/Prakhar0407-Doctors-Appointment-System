import React from "react";

const MainBody = ({ title,appointmentButton}) => {
    return (
        <>
          <div className="hero container">
           
          
          </div>
    
            <button className="appointButton">{appointmentButton}</button>
        </>
      );
};

export default MainBody;